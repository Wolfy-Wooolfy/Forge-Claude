[project]
name = "todo_cli"
version = "0.1.0"
description = "A minimal command-line TODO list manager"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

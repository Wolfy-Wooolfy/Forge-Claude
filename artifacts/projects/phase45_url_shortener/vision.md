---
project_id: phase45_url_shortener
project_name: url_shortener
domain: web_api
vision_version: 1
vision_locked: true
vision_locked_at: 2026-06-28T14:00:49.178Z
locked_by_role: owner
amendments_history: []
goals:
  primary: URL shortener — a small REST backend that turns a long URL into a generated short code and redirects visitors of that code to the original URL; tracks how many times each code is visited; supports deleting a code and rejecting invalid URLs; in-memory storage (no external DB).
  secondary: []
constraints: ["Node.js + Express","In-memory storage — no external database"]
non_goals: ["Authentication","User accounts","Persistence across restarts","Custom vanity codes","Analytics dashboards"]
---
# Vision: url_shortener

## Goal
URL shortener — a small REST backend that turns a long URL into a generated short code and redirects visitors of that code to the original URL; tracks how many times each code is visited; supports deleting a code and rejecting invalid URLs; in-memory storage (no external DB).

## Features
- Shorten a long URL: submit a URL and get back a server-generated short code plus the short link
- Resolve a short code: visiting the code redirects the client to the original long URL
- Unknown short codes return a not-found response
- Stats: look up how many times a given short code has been visited
- Delete a short code so it no longer resolves
- Validation: a missing or invalid URL is rejected with a clear error
- The visit count for a code increments on each successful resolve

## Constraints
- Node.js + Express
- In-memory storage — no external database

## Non-Goals
- Authentication
- User accounts
- Persistence across restarts
- Custom vanity codes
- Analytics dashboards

---
*Generated by Forge Idea Synthesis — confirmed by owner.*
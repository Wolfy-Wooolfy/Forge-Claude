---
project_id: phase43_notes_api
project_name: notes_api
domain: web_api
vision_version: 1
vision_locked: true
vision_locked_at: 2026-06-24T11:09:54.083Z
locked_by_role: owner
amendments_history: []
goals:
  primary: Notes API — a small REST backend for personal notes (title, body, category, tags). CRUD + list with filter-by-category and keyword search (title/body), input validation with clear error responses, in-memory storage (no external DB).
  secondary: []
constraints: ["Node.js + Express","In-memory storage — no external database"]
non_goals: ["Authentication","User accounts","Persistence across restarts","Real-time sync"]
---
# Vision: notes_api

## Goal
Notes API — a small REST backend for personal notes (title, body, category, tags). CRUD + list with filter-by-category and keyword search (title/body), input validation with clear error responses, in-memory storage (no external DB).

## Features
- POST /notes — create a note (title, body, category, tags), returns 201 with the note
- GET /notes — list notes; optional ?category= filter and ?q= keyword search (title/body)
- GET /notes/:id — fetch one note; 404 if unknown
- PUT /notes/:id — update a note; 404 if unknown
- DELETE /notes/:id — delete a note; 204; 404 if unknown
- Input validation: title required (max 200 chars); 400 with a clear error on invalid input

## Constraints
- Node.js + Express
- In-memory storage — no external database

## Non-Goals
- Authentication
- User accounts
- Persistence across restarts
- Real-time sync

---
*Generated by Forge Idea Synthesis — confirmed by owner.*
{
  "id": "T-10",
  "name": "create_note_title_length_error",
  "description": "POST /notes with a title exceeding 200 characters returns 400",
  "category": "http",
  "setup": {
    "actions": [
      {
        "type": "start_server",
        "command": "node src/server.js",
        "wait_for_port": 3000,
        "timeout_ms": 5000
      }
    ]
  },
  "execution": {
    "type": "http_request",
    "method": "POST",
    "url": "http://localhost:3000/notes",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "title": "a very long title that exceeds the maximum length of characters defined for this field to trigger a validation error. This title should be longer than 200 characters in total which is not acceptable.",
      "body": "This note should not be created due to title length.",
      "category": "Validation",
      "tags": [
        "length",
        "error"
      ]
    }
  },
  "assertions": [
    {
      "type": "http_status_equals",
      "expected": 400
    }
  ],
  "teardown": {
    "actions": [
      {
        "type": "stop_server"
      }
    ]
  },
  "metadata": {
    "covers_ac": [
      "AC-6"
    ],
    "estimated_duration_ms": 500
  }
}
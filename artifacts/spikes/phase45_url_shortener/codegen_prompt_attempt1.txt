You are a code generator. Return STRICT JSON only — no markdown, no code blocks, no prose before or after.
Generate exactly the following files and return them as this exact JSON structure:
{ "files": [ { "path": "<path>", "content": "<source code>" }, ... ] }
Files to generate: src/utils/codeGenerator.js, src/utils/urlValidator.js, src/services/shortenerService.js, src/routes.js, src/server.js
File responsibilities (from the spec):
- src/server.js: Create the server, mount all routes, and start listening on a specified port.
- src/routes.js: Define and export all route handlers for the application.
- src/services/shortenerService.js: Handle short code generation, URL mapping, and visit count management.
- src/utils/urlValidator.js: Provide URL validation functionality using validator.js.
- src/utils/codeGenerator.js: Use nanoid to generate unique short codes.
Spec: The system is a URL shortener REST backend designed to convert long URLs into short codes, manage these codes, and track visit statistics. It includes endpoints for creating short codes, resolving them to original URLs, deleting them, and retrieving visit statistics. All data is stored and managed in memory without persistence across restarts.
Design: The system is a URL shortener REST backend that converts a long URL into a server-generated short code and redirects visitors from that code to the original URL. It provides endpoints for creating, resolving, deleting short codes, and retrieving visit statistics. It validates URLs, stores data in memory, and tracks visit counts per code.
Acceptance criteria (implement EVERY one completely):
- AC-1: POST /shorten accepts a JSON payload with { 'url': '...' } and returns { 'shortCode': '...', 'shortLink': '...' } on success or { 'error': { 'message': 'Invalid URL' } } if URL validation fails.
- AC-2: GET /:code redirects to the original long URL if the short code exists or returns { 'error': { 'message': 'Not found' } } if the code is unknown.
- AC-3: DELETE /:code removes the short code and its data, returns { 'result': 'success' } on success or { 'error': { 'message': 'Not found' } } if the code doesn't exist.
- AC-4: GET /stats/:code returns { 'visits': number } indicating the visit count if the short code exists or { 'error': { 'message': 'Not found' } } if the code doesn't exist.
- AC-5: Short codes must be generated using nanoid and should be opaque, not sequential.
- AC-6: In-memory storage should keep the URL mappings and visit counts without persisting to disk or external databases.
Implement EVERY acceptance criterion completely: every route (including GET /:id to fetch a single resource by id), every status code (including 404 when a resource id is not found — for GET, PUT, and DELETE), and all entity fields. The data layer must signal found vs not-found (e.g. return null/undefined for a missing id) so the route handlers can return 404 instead of silently succeeding.
Runnability: if this is an HTTP API or web service, it MUST include a server/entry file that creates the app, mounts ALL routes, and calls app.listen(process.env.PORT || 3000) so it boots and accepts HTTP requests. Implement the entry file already in the list (e.g. src/server.js); if the list contains NO entry file (none of src/server.js, src/index.js, src/app.js, index.js, server.js, app.js), ALSO generate src/server.js with that bootstrap. Do not add persistence/backup or test files beyond the spec's declared scope.
Endpoint paths: the served paths MUST exactly equal the paths the acceptance criteria declare. When you mount a router in the entry file, the mount path joined with the router's route paths MUST equal the AC-declared paths — do NOT introduce a base-path or version prefix (e.g. /api, /v1) unless the acceptance criteria explicitly include it. If the ACs say POST /notes, the app must serve POST /notes, NOT /api/notes (e.g. app.use(router) with router.post('/notes', ...), OR app.use('/notes', router) with router.post('/', ...) — but NEVER both prefixes).
RESPOND WITH VALID JSON ONLY.
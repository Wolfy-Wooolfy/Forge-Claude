{
  "id": "S118",
  "name": "role.invoke quality_judge — REJECTED verdict when BLOCKER cross-role issue present",
  "type": "direct_tool",
  "permission": "TEST",
  "tool": "role.invoke",
  "input": {
    "role_id":     "quality_judge",
    "project_id":  "test_qj_s118",
    "provider":    "mock",
    "model":       "mock-qj-s118",
    "scenario_id": "S118",
    "input": {
      "project_id": "test_qj_s118",
      "spec": {
        "scope": "Build a task management REST API",
        "decisions": [{ "decision": "Use Express.js", "rationale": "Minimal setup" }],
        "acceptance_criteria": [
          { "id": "AC-1", "description": "POST /users returns 201 with user id" },
          { "id": "AC-2", "description": "POST /auth/login returns JWT on correct credentials" },
          { "id": "AC-3", "description": "GET /tasks requires valid JWT; returns 401 without token" }
        ],
        "files_to_create": [{ "path": "src/app.js", "purpose": "Entry point" }],
        "files_to_modify": [],
        "out_of_scope": ["Frontend UI"]
      },
      "design": {
        "design_summary": "Node.js/Express API with SQLite and JWT auth",
        "components": [{ "name": "API Server", "tech": "Express", "purpose": "HTTP handler" }],
        "data_flow": "Client → API → SQLite",
        "technology_choices": [],
        "integration_points": [],
        "identified_risks": []
      },
      "security_audit": {
        "threat_level": "HIGH",
        "findings": [
          {
            "severity": "BLOCKER",
            "vulnerability": "Missing authentication on admin endpoints",
            "location": "spec.acceptance_criteria",
            "attack_vector": "Direct access to admin route without token",
            "mitigation": "Add JWT enforcement to all admin routes"
          }
        ],
        "summary": "HIGH threat with unresolved BLOCKER; pipeline must not proceed."
      }
    }
  },
  "assertions": [
    { "type": "status_equals", "expected": "SUCCESS" },
    { "type": "output_field_exists", "field": "verdict" },
    { "type": "output_field_exists", "field": "cross_role_issues" }
  ]
}

{
  "id": "S52",
  "name": "pkg.install npm_global (Tier 2) routes to shell.run_with_prompt and is auto-denied in tests",
  "type": "direct_tool",
  "permission": "WORKSPACE_WRITE",
  "tool": "pkg.install",
  "input": {
    "adapter_id": "npm_global",
    "packages": ["is-odd"]
  },
  "assertions": [
    { "type": "status_equals",      "expected": "FAILED" },
    { "type": "state_field_exists", "field": "reason" },
    { "type": "state_field_equals", "field": "reason",  "expected": "COMMAND_DENIED" },
    { "type": "audit_count",        "min": 1 }
  ]
}

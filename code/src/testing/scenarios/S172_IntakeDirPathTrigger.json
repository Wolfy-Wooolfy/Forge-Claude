{
  "id": "S172",
  "name": "intake handler — directory_path trigger starts intake, returns AWAIT_VISION_APPROVAL",
  "description": "Stage 11.4 — processIntakeRequest with directory_path triggers _startIntake and returns stage=AWAIT_VISION_APPROVAL.",
  "type": "module_call",
  "permission": "WORKSPACE_WRITE",
  "module": "code/src/testing/helpers/intake_test_helper",
  "method": "runS172IntakeDirPathTrigger",
  "args": [],
  "cleanup_project": "test_s172_proj",
  "assertions": [
    { "type": "status_equals",      "expected": "SUCCESS" },
    { "type": "state_field_equals", "field": "stage_is_await_vision_approval", "expected": true },
    { "type": "state_field_equals", "field": "ok",                             "expected": true }
  ]
}

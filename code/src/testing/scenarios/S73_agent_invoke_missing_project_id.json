{
  "id": "S73",
  "name": "agent.invoke with missing project_id returns FAILED (INVALID_INPUT)",
  "type": "direct_tool",
  "permission": "TEST",
  "tool": "agent.invoke",
  "fixture_files": [],
  "input": {
    "provider": "mock",
    "model":    "mock",
    "prompt":   "Test prompt S73"
  },
  "assertions": [
    { "type": "status_equals",      "expected": "FAILED" },
    { "type": "state_field_equals", "field": "reason", "expected": "INVALID_INPUT" }
  ]
}
